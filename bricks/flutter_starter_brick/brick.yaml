name: flutter_starter_brick
description: A brick for bootstrapping a new flutter project with useful features.
repository: https://github.com/Haidar0096/fcu/tree/main/bricks/flutter_starter_brick

# The following defines the brick repository url.
# Uncomment and update the following line before publishing the brick.
# repository: https://github.com/my_org/my_repo

# The following defines the version and build number for your brick.
# A version number is three numbers separated by dots, like 1.2.34
# followed by an optional build number (separated by a +).
version: 0.5.0

# The following defines the environment for the current brick.
# It includes the version of mason that the brick requires.
environment:
  mason: ">=0.1.0-dev.60 <=0.1.1"

# Variables specify dynamic values that your brick depends on.
# Zero or more variables can be specified for a given brick.
# Each variable has:
#  * a type (string, number, boolean, enum, array, or list)
#  * an optional short description
#  * an optional default value
#  * an optional list of default values (array only)
#  * an optional prompt phrase used when asking for the variable
#  * a list of values (enums only)
#  * an optional separator (list only)
vars:
  dev_name:
    type: string
    description: Developer name
    default: developer
    prompt: What is your name?

  proj_name:
    type: string
    description: Project name
    default: my_project
    prompt: What is your project name?

  org_name:
    type: string
    description: Organization name
    default: my_org
    prompt: What is your organization name?

  proj_desc:
    type: string
    description: Project description
    default: A new Flutter project
    prompt: What is your project description?

  has_envs:
    type: boolean
    description: Whether the app supports multiple environments
    default: true
    prompt: Does the app support multiple environments?

  has_ic:
    type: boolean
    description: Whether the app needs internet connectivity
    default: true
    prompt: Does the app need internet connectivity?
