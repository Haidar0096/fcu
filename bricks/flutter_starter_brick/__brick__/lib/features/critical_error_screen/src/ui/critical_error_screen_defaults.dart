/// Design values owned by the critical error screen.
abstract final class CriticalErrorScreenDefaults {
  /// The oversized error glyph that carries the screen.
  static const double iconSize = 64;

  /// Softens the explanatory message against the title.
  static const double messageAlpha = 0.8;
}
