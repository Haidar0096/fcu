/// Query parameter keys used in URL navigation to maintain state
/// across redirects and deep linking.
///
/// TODO({{dev_name}}): Add your app-specific query parameter keys here
final class QueryParamKeys {
  // Private constructor to prevent instantiation
  const QueryParamKeys._();

  /// Query parameter for error message in critical error screen
  static const String errorMessage = 'error-message';
}
